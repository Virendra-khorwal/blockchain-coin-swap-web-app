import Head from "next/head";
import Card from "../components/Card";
import Button from "../util/Button";
import { IoChevronForwardOutline } from "react-icons/io5";
import { useRouter } from "next/router";
import cryptoIcon from "../asset/cryptoIcon.png";
import Image from "next/image";
import WalletModal from "../components/WalletModal";
import { TransactionContext } from "../context/TransactionContext";
import { useContext } from "react";
import LatestFeed from "../components/LatestFeed";

export default function Home() {
  const router = useRouter();
  const { connectWallet, currentAccount } = useContext(TransactionContext);

  return (
    <div className="flex grow h-full">
      <Head>
        <title>Blockchain App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex flex-col w-full gap-y-10">
        <div className=" min-w-[200px] sm:w-64" >
          <Card>
            <div className="flex flex-col items-center gap-y-10 px-10 py-10 sm:gap-y-4 sm:px-4 sm:py-1 sm:px-1 w-96">
              <h1 className="text-3xl font-semibold sm:text-xl">
                Convert to any Crypto Currency
              </h1>
              <Image src={cryptoIcon} alt="crpto icon" />
              <div className="w-full flex flex-col items-stretch static">
                {currentAccount ? (
                  <Button onClick={() => router.push("/swap")}>
                    <div className="w-full flex justify-center items-center gap-x-2">
                      <p className="text-xl sm:text-sm">Convert Now</p>
                      <IoChevronForwardOutline className="text-xl" />
                    </div>
                  </Button>
                ) : (
                  <WalletModal />
                )}
              </div>
            </div>
          </Card>
        </div>
        <LatestFeed />
      </main>
    </div>
  );
}
